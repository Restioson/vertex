{
  "name": "vector-client",
  "version": "1.0.0",
  "description": "An electron client for vector",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "cd ../backend && wasm-pack build && cd ../electron && npm run build && electron dist/main.js"
  },
  "author": "gegy1000",
  "license": "AGPL-3.0",
  "dependencies": {
    "vertex_client": "file:../backend/pkg/"
  },
  "devDependencies": {
    "electron": "^5.0.2",
    "typescript": "^2.8.3"
  }
}
